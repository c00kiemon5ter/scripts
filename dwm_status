#!/usr/bin/env bash
date="$(date +"%a %d/%m %R")"
vol="$(amixer get PCM | sed -nr "$ s:.+\[(.+%)].+:\1:gp")"
wifi="$(wireless_status)"
music="$(deadbeef --nowplaying "%a ~ %t")"
xsetroot -name "$music :: $wifi :: $vol :: $date"
