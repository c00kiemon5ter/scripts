#!/usr/bin/env bash

[[ -t 0 ]]  && url="$(curl -sF 'sprunge=<-' http://sprunge.us <"$1")" \
            || url=$(curl -sF 'sprunge=<-' http://sprunge.us)
printf "txt:%s %s\n" "$url" "$*" >> /tmp/saved.urls
xclip -i <<< "$url" && xclip -o

