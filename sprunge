#!/usr/bin/env bash

function fsprunge() {
    curl -sF 'sprunge=<-' http://sprunge.us < $1
}   

function isprunge() {
	curl -sF 'sprunge=<-' http://sprunge.us
}

[[ -t 0 ]] && execfunc="fsprunge" || execfunc="isprunge"

$execfunc $1 | tee -a /tmp/saved.urls | xclip -i && xclip -o

